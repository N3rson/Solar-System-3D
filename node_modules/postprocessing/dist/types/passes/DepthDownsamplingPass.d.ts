import { WebGLRenderer } from "three";
import { Pass } from "../core/Pass.js";
import { DepthDownsamplingMaterial } from "../materials/DepthDownsamplingMaterial.js";
import { Resolution } from "../utils/Resolution.js";
/**
 * A downsampling pass that picks the most representative depth (and normal) in 2x2 texel neighborhoods.
 *
 * @group Passes
 */
export declare class DepthDownsamplingPass extends Pass<DepthDownsamplingMaterial> {
    /**
     * Constructs a new depth downsampling pass.
     */
    constructor();
    protected onInputChange(): void;
    protected onResolutionChange(resolution: Resolution): void;
    checkRequirements(renderer: WebGLRenderer): void;
    render(): void;
}
