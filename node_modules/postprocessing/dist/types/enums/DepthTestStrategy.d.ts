/**
 * An enumeration of depth test strategies.
 *
 * @group Enums
 */
export declare enum DepthTestStrategy {
    /**
     * Perform only a depth test.
     */
    DEFAULT = 0,
    /**
     * Always keeps max depth values.
     */
    KEEP_MAX_DEPTH = 1,
    /**
     * Always discards max depth values.
     */
    DISCARD_MAX_DEPTH = 2
}
