/**
 * An enumeration of depth copy modes.
 *
 * @group Enums
 */
export declare enum DepthCopyMode {
    /**
     * Copies the full depth texture every frame.
     */
    FULL = 0,
    /**
     * Copies a single texel from the depth texture on demand.
     */
    SINGLE = 1
}
