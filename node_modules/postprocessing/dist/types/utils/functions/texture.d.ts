import { Texture } from "three";
/**
 * Creates a hash string that identifies a given texture.
 *
 * @ignore
 * @param texture - A texture.
 * @return The hash value.
 * @group Utils
 */
export declare function getHash(texture: Texture): string;
